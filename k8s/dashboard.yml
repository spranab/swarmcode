apiVersion: apps/v1
kind: Deployment
metadata:
  name: swarmcode-dashboard
  namespace: mcp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: swarmcode-dashboard
  template:
    metadata:
      labels:
        app: swarmcode-dashboard
    spec:
      imagePullSecrets:
        - name: harbor-secret
      containers:
        - name: dashboard
          image: harbor.mycluster.cyou/mcp/swarmcode-dashboard:latest
          ports:
            - containerPort: 4200
          env:
            - name: SWARMCODE_REDIS_URL
              value: redis://redis.mcp.svc.cluster.local:6379
            - name: DASHBOARD_PORT
              value: "4200"
            - name: DASHBOARD_USER
              valueFrom:
                secretKeyRef:
                  name: dashboard-auth
                  key: username
            - name: DASHBOARD_PASS
              valueFrom:
                secretKeyRef:
                  name: dashboard-auth
                  key: password
          resources:
            requests:
              memory: 64Mi
              cpu: 50m
            limits:
              memory: 256Mi
              cpu: 200m
          readinessProbe:
            httpGet:
              path: /health
              port: 4200
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: swarmcode-dashboard
  namespace: mcp
spec:
  selector:
    app: swarmcode-dashboard
  ports:
    - port: 80
      targetPort: 4200
