apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: swarmcode-tls
  namespace: mcp
spec:
  secretName: swarmcode-tls
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-cloudflare-prod
  dnsNames:
    - swarmcode.mcp.mycluster.cyou
  secretTemplate:
    labels:
      app: swarmcode-dashboard
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: swarmcode-dashboard
  namespace: mcp
  annotations:
    nginx.ingress.kubernetes.io/proxy-read-timeout: "3600"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "3600"
    nginx.ingress.kubernetes.io/proxy-buffering: "off"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - swarmcode.mcp.mycluster.cyou
      secretName: swarmcode-tls
  rules:
    - host: swarmcode.mcp.mycluster.cyou
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: swarmcode-dashboard
                port:
                  number: 80
